# Allow the app user to start the updater unit (backup if PathExists misses a rewrite)
veninspect ALL=(root) NOPASSWD: /bin/systemctl start veninspect-update.service, /bin/systemctl reset-failed veninspect-update.service, /bin/systemctl is-active veninspect-update.service
